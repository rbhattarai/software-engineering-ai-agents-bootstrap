# Project memory index

One line per entry: `- [title](file.md) — hook`. This index is always loaded (cheap);
the linked files are read on demand. Keep entries to one line — content lives in the files.

<!-- entries added by agents as durable learnings accumulate -->
